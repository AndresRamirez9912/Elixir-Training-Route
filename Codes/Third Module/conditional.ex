age = 25

# If using KeyWord List
result = if age >= 18, do: "adult", else: "Teenager"
IO.puts(result)

# Notice If I have small code inside the if I can type the If result after do:
# the : after do means that do is a keyword list and the result is the value
